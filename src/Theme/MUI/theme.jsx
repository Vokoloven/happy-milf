import { createTheme } from '@mui/material';
export const theme = createTheme({
  palette: {
    primary: {
      main: '#FC842D', //this overide blue color
      // light: "your own color", //overides light blue
      // dark: "your own color", //overides dark blue color
    },
  },
});
